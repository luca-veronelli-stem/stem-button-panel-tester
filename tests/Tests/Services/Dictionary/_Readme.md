# Tests / Services / Dictionary

F# tests for `DictionaryService` orchestration live in
`tests/Tests.FSharp/Services/Dictionary/` (sibling-project pattern).

This directory is reserved for any C# integration tests that exercise the
service layer indirectly.
