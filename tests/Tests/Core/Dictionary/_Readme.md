# Tests / Core / Dictionary

C# tests for `Core.FSharp.Dictionary` types live in `tests/Tests.FSharp/Core/Dictionary/`
(F# tests in their own project; sibling-project pattern matching `src/Core.FSharp`).

This directory is reserved for any C# tests that exercise Core types directly.
