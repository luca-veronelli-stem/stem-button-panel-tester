# Tests / Integration / Dictionary

End-to-end tests using `WireMock.Net` + temp filesystem. Lands with US2
(T063 in tasks.md): cold-start (no cache, no credential), live success path,
break-API-then-restart cached path.
