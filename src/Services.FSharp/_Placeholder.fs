namespace Stem.ButtonPanel.Tester.Services

/// Phase 3 beachhead. The first real type in this project lands with US2's
/// `DictionaryService` (T073). This placeholder exists so the project compiles
/// in PR #1 (Setup + Foundational) ahead of the orchestrator landing.
module internal Placeholder =
    let internal beachhead = "Phase 3"
